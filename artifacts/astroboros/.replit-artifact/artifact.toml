kind = "web"
previewPath = "/"
title = "AstroAlchemy"
version = "1.0.0"
id = "artifacts/astroboros"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/astroboros run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/astroboros", "run", "build" ]
serve = "static"
publicDir = "artifacts/astroboros/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
